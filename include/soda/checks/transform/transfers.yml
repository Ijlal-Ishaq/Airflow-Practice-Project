checks for transfers:
  - schema:
      fail:
        when required column missing: [from_address, to_address, value]
        when wrong column type:
          from_address: string
          to_address: string
          value: numeric
